services:
    db:
        image: postgres
        container_name: chronos-db
        ports:
            - "5433:5432"
        environment:
            POSTGRES_USER: user
            POSTGRES_PASSWORD: password
            POSTGRES_DB: db
